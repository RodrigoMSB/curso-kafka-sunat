# Esta guía se movió

El Lab 14 ahora tiene **una sola guía**:

### → [`01-seguridad-y-lo-que-rechaza.md`](01-seguridad-y-lo-que-rechaza.md)

Lo que antes estaba repartido en seis archivos (`01-tls-y-certificados`,
`02-sasl-autenticacion`, `03-acls-autorizacion`, `04-min-insync-replicas`,
`05-simulacion-fallo-recuperacion` y `06-capstone-automatizado`) está ahí: el
recorrido principal en la sección **5 · LOS PASOS**, y el resto —la PKI, escribir
ACLs a mano, el drill de failover y el capstone automatizado— en la sección
**7 · PARA PROFUNDIZAR**, con su comando completo y su salida medida.

Los originales quedaron en `_fuente-extra/guia/`.

> Este archivo existe solo porque `bin/start-lab.sh` todavía imprime el nombre
> antiguo al terminar. Se puede borrar en cuanto esa línea se actualice.
