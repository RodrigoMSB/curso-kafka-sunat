# Esta guía se movió

El Lab 13 ahora tiene **una sola guía**:

### → [`01-connect-y-quien-creo-ese-topico.md`](01-connect-y-quien-creo-ese-topico.md)

Lo que antes estaba repartido en cuatro archivos (`01-arquitectura-connect`,
`02-source-jdbc`, `03-sink-jdbc` y `04-desafio-flow-completo`) está ahí: el
recorrido principal en la sección **5 · LOS PASOS**, y el resto de las
actividades —verificar Connect, los plugins, el Sink completo, la inserción
masiva y los offsets— en la sección **7 · PARA PROFUNDIZAR**, con su comando
completo y su salida medida.

Los originales quedaron en `_fuente-extra/guia/`.

> Este archivo existe solo porque `bin/start-lab.sh` todavía imprime el nombre
> antiguo al terminar. Se puede borrar en cuanto esa línea se actualice.
