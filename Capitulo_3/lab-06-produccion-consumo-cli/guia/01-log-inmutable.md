# Esta guía se movió

El Lab 06 ahora tiene **una sola guía**:

### → [`01-grupos-y-quien-repartio-el-trabajo.md`](01-grupos-y-quien-repartio-el-trabajo.md)

Lo que antes estaba repartido en tres archivos (`01-log-inmutable`,
`02-pubsub-multiples-consumidores`, `03-offsets-y-replay`) está ahí: el
recorrido principal en la sección **5 · LOS PASOS**, y el resto en la sección
**7 · PARA PROFUNDIZAR**, con su comando completo.

> Este archivo existe solo porque `bin/start-lab.sh` todavía imprime el nombre
> antiguo al terminar. Se puede borrar en cuanto esa línea se actualice.
